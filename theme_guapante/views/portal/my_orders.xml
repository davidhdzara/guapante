<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="portal_my_orders_guapante" inherit_id="sale.portal_my_orders" name="Guapante: My Orders" priority="99">
        <xpath expr="//t[@t-call='portal.portal_layout']" position="replace">
            <t t-call="portal.portal_layout">
                <t t-set="breadcrumbs_searchbar" t-value="True"/>

                <!-- Guapante Custom Layout with Sidebar -->
                <t t-call="theme_guapante.guapante_portal_layout">

                    <!-- HEADER -->
                    <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center mb-4">
                        <div>
                            <h2 class="fw-bold text-dark mb-1">Historial de Pedidos</h2>
                            <p class="text-muted mb-0">Consulta, rastrea y repite tus pedidos anteriores.</p>
                        </div>
                    </div>

                    <!-- KPI Cards -->
                    <div class="row g-3 mb-4">
                        <div class="col-12 col-md-6">
                            <div class="card border-0 shadow-sm h-100">
                                <div class="card-body d-flex align-items-center p-4">
                                    <div class="bg-light rounded-circle d-flex align-items-center justify-content-center me-3" style="width: 48px; height: 48px;">
                                        <i class="fa fa-shopping-bag text-secondary"></i>
                                    </div>
                                    <div>
                                        <small class="text-muted text-uppercase fw-bold" style="font-size: 0.7rem;">Total Pedidos</small>
                                        <h3 class="fw-bold mb-0"><t t-esc="total_orders_count or 0"/></h3>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-12 col-md-6">
                            <div class="card border-0 shadow-sm h-100">
                                <div class="card-body d-flex align-items-center p-4">
                                    <div class="rounded-circle d-flex align-items-center justify-content-center me-3" style="width: 48px; height: 48px; background: rgba(25,135,84,0.1);">
                                        <i class="fa fa-truck text-success"></i>
                                    </div>
                                    <div>
                                        <small class="text-muted text-uppercase fw-bold" style="font-size: 0.7rem;">Envíos Activos</small>
                                        <h3 class="fw-bold mb-0"><t t-esc="active_shipments_count or 0"/></h3>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- QUICK TABS -->
                    <t t-if="tab_counts">
                        <div class="d-flex flex-wrap gap-2 mb-4">
                            <a t-attf-href="/my/orders?filterby=all&amp;date_filter=#{date_filter}&amp;search=#{search}" 
                               t-att-class="'btn btn-sm rounded-pill px-3 fw-bold ' + ('btn-dark' if filterby == 'all' or not filterby else 'bg-white border text-muted')">
                                Todos <span class="badge bg-secondary rounded-pill ms-1"><t t-esc="tab_counts.get('all', 0)"/></span>
                            </a>
                            <a t-attf-href="/my/orders?filterby=received&amp;date_filter=#{date_filter}&amp;search=#{search}" 
                               t-att-class="'btn btn-sm rounded-pill px-3 fw-bold ' + ('btn-dark' if filterby == 'received' else 'bg-white border text-muted')">
                                Recibidos <span class="badge bg-secondary rounded-pill ms-1"><t t-esc="tab_counts.get('received', 0)"/></span>
                            </a>
                            <a t-attf-href="/my/orders?filterby=preparing&amp;date_filter=#{date_filter}&amp;search=#{search}" 
                               t-att-class="'btn btn-sm rounded-pill px-3 fw-bold ' + ('btn-dark' if filterby == 'preparing' else 'bg-white border text-muted')">
                                Preparando <span class="badge bg-secondary rounded-pill ms-1"><t t-esc="tab_counts.get('preparing', 0)"/></span>
                            </a>
                            <a t-attf-href="/my/orders?filterby=shipping&amp;date_filter=#{date_filter}&amp;search=#{search}" 
                               t-att-class="'btn btn-sm rounded-pill px-3 fw-bold ' + ('btn-dark' if filterby == 'shipping' else 'bg-white border text-muted')">
                                En Camino <span class="badge bg-secondary rounded-pill ms-1"><t t-esc="tab_counts.get('shipping', 0)"/></span>
                            </a>
                        </div>
                    </t>

                    <!-- FILTERS TOOLBAR -->
                    <form action="/my/orders" method="get" class="d-flex flex-column flex-md-row align-items-center mb-4 gap-3">
                        <input type="hidden" name="date_filter" t-att-value="date_filter"/>
                        <input type="hidden" name="filterby" t-att-value="filterby"/>

                        <!-- Search -->
                        <div class="input-group shadow-sm" style="max-width: 300px; border-radius: 50rem; overflow: hidden; border: 1px solid #dee2e6;">
                            <span class="input-group-text bg-white border-0 pe-1">
                                <i class="fa fa-search text-muted"></i>
                            </span>
                            <input type="text" name="search" t-att-value="search" class="form-control border-0 shadow-none" placeholder="Num de pedido..." aria-label="Buscar..."/>
                        </div>

                        <!-- Filter 1: Date Range -->
                        <div class="dropdown" t-if="date_range_filters">
                            <button class="btn guapante-filter-btn dropdown-toggle fw-semibold d-flex align-items-center gap-2" type="button" data-bs-toggle="dropdown" style="min-width: 190px;">
                                <i class="fa fa-calendar-o"></i>
                                <span t-esc="date_range_filters.get(date_filter, {}).get('label', 'Todo el tiempo')"/>
                            </button>
                            <ul class="dropdown-menu border-0 shadow">
                                <t t-foreach="date_range_filters" t-as="df">
                                    <li>
                                        <a t-attf-href="/my/orders?filterby=#{filterby}&amp;date_filter=#{df}&amp;search=#{search}"
                                           t-att-class="'dropdown-item' + (' active' if date_filter == df else '')">
                                            <span t-esc="date_range_filters[df].get('label')"/>
                                        </a>
                                    </li>
                                </t>
                            </ul>
                        </div>

                        <!-- Filter 2: Status -->
                        <div class="dropdown" t-if="searchbar_filters">
                            <button class="btn guapante-filter-btn dropdown-toggle fw-semibold d-flex align-items-center gap-2" type="button" data-bs-toggle="dropdown" style="min-width: 190px;">
                                <i class="fa fa-filter"></i>
                                <span t-esc="searchbar_filters.get(filterby, {}).get('label', 'Todos')"/>
                            </button>
                            <ul class="dropdown-menu border-0 shadow">
                                <t t-foreach="searchbar_filters" t-as="option">
                                    <li>
                                        <a t-attf-href="/my/orders?filterby=#{option}&amp;date_filter=#{date_filter}&amp;search=#{search}"
                                           t-att-class="'dropdown-item' + (' active' if filterby == option else '')">
                                            <span t-esc="searchbar_filters[option].get('label')"/>
                                        </a>
                                    </li>
                                </t>
                            </ul>
                        </div>

                        <!-- Clear Filters -->
                        <div class="ms-md-auto d-flex align-items-center gap-3 pe-2">
                            <a href="/my/orders" class="text-decoration-none text-success small fw-bold mt-2 mt-md-0 d-flex align-items-center gap-1" title="Limpiar filtros">
                                <i class="fa fa-times-circle"></i> Limpiar filtros
                            </a>
                            <button type="submit" class="btn btn-success rounded-pill px-4 fw-bold shadow-sm d-none d-md-block">Buscar</button>
                        </div>
                    </form>

                    <!-- ORDERS TABLE -->
                    <t t-if="orders">
                        <div class="card border-0 shadow-sm overflow-hidden">
                            <div class="table-responsive">
                                <table class="table table-hover align-middle mb-0">
                                    <thead class="bg-light text-muted small text-uppercase">
                                        <tr>
                                            <th class="py-3 ps-4 border-0">ID Pedido</th>
                                            <th class="py-3 border-0">Fecha</th>
                                            <th class="py-3 border-0">Estado</th>
                                            <th class="py-3 border-0 text-end">Total</th>
                                            <th class="py-3 border-0">Productos</th>
                                            <th class="py-3 pe-4 border-0 text-end">Acciones</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <t t-foreach="orders" t-as="order">
                                            <tr>
                                                <td class="ps-4 fw-bold text-dark">
                                                    <t t-esc="order.name"/>
                                                </td>
                                                <td class="text-muted">
                                                    <span t-field="order.date_order" t-options='{"widget": "date"}'/>
                                                </td>
                                                <td>
                                                    <t t-if="order.state == 'cancel'">
                                                        <span class="guapante-badge guapante-badge-cancelled">
                                                            <i class="fa fa-times me-1"></i>Cancelado
                                                        </span>
                                                    </t>
                                                    <t t-elif="order.guapante_delivery_status == 'delivered'">
                                                        <span class="guapante-badge guapante-badge-delivered">
                                                            <i class="fa fa-check me-1"></i>Entregado
                                                        </span>
                                                    </t>
                                                    <t t-elif="order.guapante_delivery_status == 'shipping'">
                                                        <span class="guapante-badge guapante-badge-shipping">
                                                            <i class="fa fa-truck me-1"></i>En Camino
                                                        </span>
                                                    </t>
                                                    <t t-elif="order.guapante_delivery_status == 'preparing'">
                                                        <span class="guapante-badge guapante-badge-preparing">
                                                            <i class="fa fa-clock-o me-1"></i>Preparando
                                                        </span>
                                                    </t>
                                                    <t t-else="">
                                                        <span class="guapante-badge guapante-badge-received">
                                                            Recibido
                                                        </span>
                                                    </t>
                                                </td>
                                                <td class="fw-bold text-end">
                                                    <t t-set="valid_invoices" t-value="order.invoice_ids.filtered(lambda i: i.state not in ('draft', 'cancel'))"/>
                                                    <t t-if="valid_invoices">
                                                        <span t-esc="sum(valid_invoices.mapped('amount_total_in_currency_signed'))" t-options='{"widget": "monetary", "display_currency": order.currency_id}'/>
                                                    </t>
                                                    <t t-elif="order.guapante_delivery_status in ['shipping', 'delivered']">
                                                        <span t-field="order.amount_total" t-options='{"widget": "monetary", "display_currency": order.currency_id}'/>
                                                    </t>
                                                    <t t-else="">
                                                        <span class="text-muted small fst-italic" title="El total se calculará una vez finalizado el pesaje">
                                                            Calculando... <i class="fa fa-calculator ms-1"></i>
                                                        </span>
                                                    </t>
                                                </td>
                                                <td>
                                                    <div class="d-flex align-items-center">
                                                        <t t-foreach="order.order_line[:3]" t-as="line">
                                                            <t t-if="line.product_id.image_128">
                                                                <img t-att-src="image_data_uri(line.product_id.image_128)"
                                                                     class="rounded-circle border border-2 border-white shadow-sm"
                                                                     t-attf-style="width: 32px; height: 32px; object-fit: cover; margin-right: -8px; position: relative; z-index: #{10 - line_index};"
                                                                     t-att-alt="line.product_id.name"
                                                                     t-att-title="line.product_id.name"/>
                                                            </t>
                                                            <t t-else="">
                                                                <img src="/web/static/img/placeholder.png"
                                                                     class="rounded-circle border border-2 border-white shadow-sm bg-light"
                                                                     t-attf-style="width: 32px; height: 32px; object-fit: cover; margin-right: -8px; position: relative; z-index: #{10 - line_index};"
                                                                     t-att-alt="line.product_id.name"
                                                                     t-att-title="line.product_id.name"/>
                                                            </t>
                                                        </t>
                                                        <t t-if="len(order.order_line) > 3">
                                                            <span class="rounded-circle bg-light border border-2 border-white shadow-sm d-flex align-items-center justify-content-center fw-bold text-muted"
                                                                  style="width: 32px; height: 32px; font-size: 0.65rem; position: relative; z-index: 0;"
                                                                  t-att-title="'Y ' + str(len(order.order_line) - 3) + ' productos adicionales'">
                                                                +<t t-esc="len(order.order_line) - 3"/>
                                                            </span>
                                                        </t>
                                                        <span t-if="not order.order_line" class="text-muted small fst-italic">Sin productos</span>
                                                    </div>
                                                </td>
                                                <td class="pe-4 text-end">
                                                    <div class="d-flex justify-content-end gap-2 align-items-center">
                                                        <!-- Descargar PDF WhatsApp -->
                                                        <t t-if="order.state != 'cancel'">
                                                            <a t-attf-href="/my/orders/download_whatsapp/#{order.id}" class="text-secondary text-decoration-none me-2" title="Descargar PDF (WhatsApp)">
                                                                <i class="fa fa-file-pdf-o" style="font-size: 1.1rem; opacity: 0.6;"></i>
                                                            </a>
                                                        </t>

                                                        <!-- Delivered: Reorder only -->
                                                        <t t-if="order.state != 'cancel' and order.guapante_delivery_status == 'delivered'">
                                                            <a t-attf-href="/my/orders/reorder/#{order.id}" class="btn btn-success btn-sm rounded-pill fw-bold text-white px-3">
                                                                <i class="fa fa-refresh me-1"></i> Volver a pedir
                                                            </a>
                                                        </t>

                                                        <!-- Received: Track + Cancel -->
                                                        <t t-elif="order.state == 'sale' and order.guapante_delivery_status == 'received'">
                                                            <a t-attf-href="/shop/order/status/#{order.id}" class="btn guapante-btn-track btn-sm rounded-pill fw-bold px-3">
                                                                <i class="fa fa-truck me-1"></i> Rastrear
                                                            </a>
                                                            <form t-attf-action="/my/orders/cancel/#{order.id}" method="post" class="d-inline">
                                                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                                <button type="submit" class="btn guapante-btn-cancel btn-sm rounded-pill fw-bold px-3"
                                                                        onclick="return confirm('Para retirar o cambiar un producto, debes cancelar toda la orden y volver a enviarla a tu carrito. ¿Deseas cancelar esta orden ahora?');">
                                                                    Cancelar orden
                                                                </button>
                                                            </form>
                                                        </t>

                                                        <!-- Preparing / Shipping: Track only (no cancel) -->
                                                        <t t-elif="order.state != 'cancel'">
                                                            <a t-attf-href="/shop/order/status/#{order.id}" class="btn guapante-btn-track btn-sm rounded-pill fw-bold px-3">
                                                                <i class="fa fa-truck me-1"></i> Rastrear
                                                            </a>
                                                        </t>

                                                        <!-- Cancelled: Details link -->
                                                        <t t-else="">
                                                            <div class="d-flex flex-column align-items-end gap-1">
                                                                <span class="text-muted small fw-bold">Cancelada</span>
                                                                <a t-attf-href="/my/orders/reorder/#{order.id}" class="btn border btn-sm rounded-pill fw-bold text-dark px-3 mt-1" style="font-size: 0.75rem;">
                                                                    <i class="fa fa-shopping-cart me-1 text-success"></i> Edítala en el Carrito
                                                                </a>
                                                            </div>
                                                        </t>
                                                    </div>
                                                </td>
                                            </tr>
                                        </t>
                                    </tbody>
                                </table>
                            </div>
                            <div t-if="pager" class="card-footer bg-white py-3 d-flex justify-content-between align-items-center px-4">
                                <span class="text-muted small">
                                    Mostrando <b t-esc="page_start_num"/> a <b t-esc="page_end_num"/> de <b t-esc="order_count"/> resultados
                                </span>
                                <t t-call="portal.pager"/>
                            </div>
                        </div>
                    </t>

                    <!-- Empty State -->
                    <t t-else="">
                        <div class="card border-0 shadow-sm text-center p-5">
                            <div class="mb-3">
                                <i class="fa fa-shopping-bag fa-3x text-muted opacity-50"></i>
                            </div>
                            <h5 class="fw-bold text-dark">No tienes pedidos aún</h5>
                            <p class="text-muted mb-4">¡Explora nuestra tienda y realiza tu primer pedido!</p>
                            <a href="/shop" class="btn btn-success rounded-pill px-4 fw-bold">Ir a la tienda</a>
                        </div>
                    </t>

                    <!-- HELP BANNER -->
                    <div class="guapante-help-banner mt-4 rounded-4 p-4 d-flex flex-column flex-md-row align-items-center justify-content-between" style="background: #1a1a2e; color: white;">
                        <div class="mb-3 mb-md-0">
                            <h5 class="fw-bold mb-1" style="color: white;">¿Necesitas ayuda con tu pedido?</h5>
                            <p class="mb-0 opacity-75" style="font-size: 0.9rem;">Nuestro equipo de logística está disponible para resolver cualquier duda sobre tus entregas activas.</p>
                        </div>
                        <a href="/contactus" class="btn btn-success rounded-pill px-4 fw-bold d-flex align-items-center gap-2 flex-shrink-0">
                            <i class="fa fa-headphones"></i> Contactar Soporte
                        </a>
                    </div>

                </t>
            </t>
        </xpath>
    </template>
</odoo>
