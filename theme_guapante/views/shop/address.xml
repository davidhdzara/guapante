<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="address_guapante_colombia" inherit_id="website_sale.address" name="Guapante - Force VAT field for Colombia">
        <!-- 
            Fix for Colombian localization (l10n_co):
            The VAT/NIT field is REQUIRED by DIAN regulations but Odoo hides it 
            unless website._display_partner_b2b_fields() returns True.
            We force it to always show for Colombian users.
        -->
        <xpath expr="//t[@t-if='website._display_partner_b2b_fields()']" position="replace">
            <t t-if="website._display_partner_b2b_fields() or res_company.country_id.code == 'CO'">
                <div class="w-100"/>
                <t t-if="show_vat">
                    <t t-set="vat_warning" t-value="partner_sudo.vat and not can_edit_vat"/>
                    <div id="company_name_div" class="col-lg-6 mb-2">
                        <label class="col-form-label fw-normal label-optional" for="o_company_name">
                            Company Name
                        </label>
                        <input id="o_company_name" type="text" name="company_name" t-att-value="partner_sudo.commercial_company_name" t-att-readonly="'1' if vat_warning else None" class="form-control"/>
                        <small t-if="vat_warning" class="form-text text-muted d-block d-lg-none">
                            Changing company name is not allowed once document(s) have been issued for your account. Please contact us directly for this operation.
                        </small>
                    </div>
                    <div id="div_vat" class="col-lg-6 mb-2">
                        <label class="col-form-label" for="o_vat">
                            <t t-out="vat_label"/>
                        </label>
                        <input type="text" id="o_vat" name="vat" t-att-value="partner_sudo.vat" t-att-readonly="'1' if vat_warning else None" class="form-control" required="required"/>
                        <small t-if="vat_warning" class="form-text text-muted d-block d-lg-none">
                            Changing VAT number is not allowed once document(s) have been issued for your account. Please contact us directly for this operation.
                        </small>
                    </div>
                    <div t-if="vat_warning" class="col-12 d-none d-lg-block mb-1">
                        <small class="form-text text-muted">
                            Changing company name or VAT number is not allowed once document(s) have been issued for your account. Please contact us directly for this operation.
                        </small>
                    </div>
                </t>
            </t>
        </xpath>
    </template>
</odoo>
